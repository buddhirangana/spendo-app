<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.Spendo" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@color/primary_green</item>
        <item name="colorPrimary">@color/primary_green</item>
        <item name="colorAccent">@color/primary_green</item>
    </style>

    <style name="Theme.Spendo" parent="Base.Theme.Spendo" />
    <!-- Base application theme. -->
    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/green</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/spendo_logo_v1</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.Spendo</item>
    </style>
</resources>